# Sub2API 用量

ZTools 插件，用于查看 Sub2API API Key token 使用量，并支持置顶悬浮小窗。

## 功能

- 配置请求域名
- 配置 `authorization` 请求头
- 配置多个 `api_key_ids`，每个 ID 可设置别名
- 拉取 `/api/v1/usage/dashboard/api-keys-usage` 用量数据
- 打开透明、无边框、置顶的悬浮小窗

## 开发

```bash
npm install
npm run dev
npm run build
```
